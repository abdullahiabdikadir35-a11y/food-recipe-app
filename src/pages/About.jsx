
export default function About() {
  return (
    <section className="about">
      <h1 className="about__title">About</h1>
      <p className="about__text">
        This is a Vite React single-page application using React Router and a
        mock REST API (json-server). We demonstrate GET, POST, PATCH, and DELETE
        with proper state updates in React.
      </p>
    </section>
  );
}
